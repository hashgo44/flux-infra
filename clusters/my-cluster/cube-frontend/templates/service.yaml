apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default (include "cube-frontend.fullname" .) }}
  labels:
    {{- include "cube-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cube-frontend.selectorLabels" . | nindent 4 }}
