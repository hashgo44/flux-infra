apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  - ../../base/cube-backend
  - image-repository.yaml
  - image-policy.yaml
  - image-update.yaml
patchesStrategicMerge:
  - release.yaml
patches:
  - target:
      kind: HelmRelease
      name: cube-backend
    patch: |-
      - op: replace
        path: /metadata/name
        value: cube-backend-prod
configMapGenerator:
  - name: cube-backend-values-production
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true

